# pandas_to_postgresql
python module written to make interaction between pandas library and postgresql easier. I.e. it allows to insert (and update) an entire data frame to table and load table directly as pandas DataFrame
